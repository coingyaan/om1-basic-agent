# OM1 Basic Agent

This repository contains a simple beginner-friendly Python agent created as part of learning OM1, OpenMind’s open-source runtime for AI agents and machines.

## What this agent does

When executed, the agent:
- Prints a greeting message
- Confirms the agent is running
- Displays the current system time
- Loads the OpenMind API key securely from environment variables

## How to run

1. Open a terminal in this folder  
2. Install dependencies:
   ```bash
   pip install python-dotenv requests
